interface:
  display_name: "Rail Connector Operator"
  short_description: "Operate persistent Rail Connector sessions"
  default_prompt: "Use $rail-operator to run an explicitly configured Rail Connector session through start, prompt, wait, and cleanup."
